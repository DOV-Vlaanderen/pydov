build:
  image: latest

python:
  version: 3.6
  setup_py_install: true

# Don't build any extra formats apart from json/html
formats: []

# Add specific dependencies for the jupyter notebooks
requirements_file: requirements_doc.txt
